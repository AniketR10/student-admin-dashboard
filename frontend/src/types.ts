export interface Student {
    ID: number;
    name: string;
    email: string;
    course: string;
}

export interface StudentForm {
  name: string;
  email: string;
  course: string;
  password: string;
}